/**
 * @deprecated Prefer `SfSymbol` from `@/components/brand`. Kept as a thin
 * alias so older call sites keep compiling while marketing migrates.
 */
export {
  SfSymbol as ForayIcon,
  SF_SYMBOL_SIZE as SIZE,
  type SfSymbolName as ForayIconName,
  type SfSymbolSize as ForayIconSize,
} from "./sf-symbol";
